# Omega ← Gaia protocol

Accept only jobs matching JuniorLLM `junior_osai/schema/omega.job.json`.
Jobs: dash-viewport | gaia-spine | terrain-obj | agi-capsule.
`launch` is always false here. Mesh is obj/ply from `~/.juniorhome/gaia_mesh/`.
