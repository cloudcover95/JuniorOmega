# Legacy drawing lab (JuniorOmega + JuniorLLM)

CAD that already lived here: `cad/interop_engine.py` (STEP/STL/3DM → mesh tensors).
This pack is the **paper → 2D → 3D intent** path. It does not replace interop.

Do not dump 40 drawings. Run `pilot` on one sheet first.
